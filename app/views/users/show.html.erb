<h4><%= @user.name %>さんのプロフィールは次のとおりです</h4>
<p>メールアドレス：<%= @user.email %></p>
<p><%= @user.name %>さんのプロフィール画像</p>
<p><%= image_tag @user.image.url %></p>

<% if current_user.id == @user.id %>
  <p><%= link_to 'プロフィールを編集する', edit_user_path(@user) %> </p>
<% end %>

<% if logged_in? %>
  <p><%= link_to '一覧へ', feeds_path %></p>
  <p><%= link_to '新規投稿へ', new_feed_path %></p>
<% end %>
